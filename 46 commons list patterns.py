basket = ['a', 'x', 'b', 'c', 'd', 'e']
basket.sort()
basket.reverse()
print(basket)

print(basket[:3])
print(basket)

print(list(range(1,100)))


sentence = ' '

new_sentence = sentence.join(['hi', 'my', 'name', 'is', 'bambo']) # It creates a new item for us so we need to assign it to a new variable, It combines a list to a string

print(sentence)
print(new_sentence)