import datetime
from array import array

print(datetime.time(5,45,2))

arr = array('i', [1,2,3]) # i - type of the data we're going to hold so integer
#This has a better performance than a list
print(arr[0])